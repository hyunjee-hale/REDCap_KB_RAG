# macOS metadata
.DS_Store
**/.DS_Store

# Local instance configuration — never commit real paths, tokens, or credentials
config.local.yaml
.env
*.local.yaml

# Python cache
__pycache__/
*.py[cod]

# Local tooling — not for distribution
tools/

site/
